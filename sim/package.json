{
  "name": "@arena/sim",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tournament simulator: fast-forwards the remaining bracket and emits results.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@arena/contracts": "*",
    "fastify": "^5.3.2",
    "@fastify/cors": "^11.0.1"
  }
}
