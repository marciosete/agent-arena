{
  "name": "@arena/pricing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pricing engine: models fixture probabilities and publishes markets with odds.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@arena/contracts": "*",
    "fastify": "^5.3.2",
    "@fastify/cors": "^11.0.1"
  }
}
