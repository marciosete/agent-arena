{
  "name": "@arena/bots",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous punter agents that research, price and bet into the Arena markets.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.5",
    "@arena/contracts": "*",
    "@arena/service-auth": "*"
  }
}
